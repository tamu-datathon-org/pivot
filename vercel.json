{
  "builds": [
    { "src": "package.json", "use": "@now/next" },
    { "src": "/api/**", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/forms(.*)", "dest": "/api/forms/$1" },
    { "src": "/(.*)", "dest": "https://dev.tamudatathon.com/$1" }
  ]
}